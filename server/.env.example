# Copy to .env and fill in your values. Safe to commit — no secrets.

DATABASE_URL=postgresql://user:password@hostname:5432/database_name
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
