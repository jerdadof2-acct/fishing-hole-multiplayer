/** Clicks on WAITING… within this window count toward a scold. */
export const WAITING_SPAM_WINDOW_MS = 2500;

/** How many ignored waiting taps before Halley turns around. */
export const WAITING_SPAM_CLICK_THRESHOLD = 3;

/** Minimum time between scold reactions. */
export const WAITING_SCOLD_COOLDOWN_MS = 14000;

/** How long Halley stays turned toward the player. */
export const WAITING_SCOLD_TURN_MS = 3600;

export const WAITING_SCOLD_LINES = [
    'The WAITING button is not a drum solo, captain.',
    'Spamming WAITING won\'t make the fish bite faster — or your hook time look legit.',
    'Nice try. Reaction times count at SET HOOK, not while you panic-tap the creek.',
    'This isn\'t speed-running a loading screen. Let the bobber do its job.',
    'The speed board knows when you\'re cheating. So do I.',
    'Patience, angler. The fish file complaints when you button-mash.',
    'WAITING means wait — not mash until the leaderboard thinks you\'re superhuman.',
    'Want a fast time? Be ready when the bobber dips, not when the text says WAITING.'
];
