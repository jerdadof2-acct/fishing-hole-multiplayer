/** Clicks on WAITING… within this window count toward a scold. */
export const WAITING_SPAM_WINDOW_MS = 2500;

/** How many ignored waiting taps before Halley turns around. */
export const WAITING_SPAM_CLICK_THRESHOLD = 3;

/** Minimum time between scold reactions. */
export const WAITING_SCOLD_COOLDOWN_MS = 14000;

/** How long Halley stays turned toward the player. */
export const WAITING_SCOLD_TURN_MS = 3600;

export const WAITING_SCOLD_LINES = [
    'Halley turns around. "The WAITING button is not a drum solo, captain."',
    'Halley squints at you. "Spamming WAITING won\'t make the fish bite faster — or your hook time look legit."',
    'Halley folds his paws. "Nice try. Reaction times are measured at SET HOOK, not while you panic-tap the creek."',
    'Halley sighs. "This isn\'t speed-running a loading screen. Let the bobber do its job."',
    'Halley whispers, "The speed board can tell when you\'re cheating. So can I. I have nine lives and one disappointed stare."',
    'Halley clears his throat. "Patience, angler. The fish file complaints when you button-mash."',
    'Halley taps the rod. "WAITING means wait. Not \'click until the leaderboard believes you\'re superhuman.\'"',
    'Halley stares. "If you want a fast time, be ready when the bobber dips — not when the text says WAITING."'
];
